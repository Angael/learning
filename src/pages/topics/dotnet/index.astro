---
import TopicPage from '../../../components/TopicPage.astro';
import { topics } from '../../../data/catalog';
const topic = topics.find((item) => item.slug === 'dotnet');
if (!topic) throw new Error('Missing .NET topic');
---

<TopicPage topic={topic} />
