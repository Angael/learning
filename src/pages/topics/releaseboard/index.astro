---
import TopicPage from '../../../components/TopicPage.astro';
import { topics } from '../../../data/catalog';
const topic = topics.find((item) => item.slug === 'releaseboard');
if (!topic) throw new Error('Missing ReleaseBoard topic');
---

<TopicPage topic={topic} />
