---
type Session = {
  href: string;
  title: string;
  summary: string;
  date?: string;
  completed: boolean;
};

type Props = {
  sessions: Session[];
  ariaLabel?: string;
};

const { sessions, ariaLabel } = Astro.props;
const sessionNumber = (href: string) => href.match(/sessions\/(\d+)/)?.[1] ?? '';
---

{sessions.length > 0 ? (
  <ul class="session-list" aria-label={ariaLabel}>
    {sessions.map((session) => (
      <li>
        <a class="session-card" href={session.href}>
          <small>
            <span>Session {sessionNumber(session.href)} · {session.date ?? 'Undated'}</span>
            {session.completed && <span class="completion-mark" aria-label="Completed" title="Completed">✓</span>}
          </small>
          <strong>{session.title}</strong>
          <span>{session.summary}</span>
        </a>
      </li>
    ))}
  </ul>
) : (
  <p class="empty-card">No sessions published yet.</p>
)}

<style>
  .session-list {
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .session-card,
  .empty-card {
    display: block;
    padding: 14px 16px;
    border: 1px solid var(--line);
    border-radius: 14px;
    background: var(--panel);
  }

  .session-card small {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    margin-bottom: 4px;
    color: var(--muted);
    font-size: .82rem;
    font-weight: 650;
  }

  .completion-mark {
    display: inline-grid;
    flex: 0 0 auto;
    place-items: center;
    width: 1.3rem;
    height: 1.3rem;
    border: 1px solid color-mix(in srgb, #2c9b64 38%, var(--line));
    border-radius: 50%;
    background: color-mix(in srgb, #2c9b64 13%, var(--panel));
    color: #207a4d;
    font-size: .92rem;
    font-weight: 800;
    line-height: 1;
  }

  .session-card strong {
    display: block;
    margin-bottom: 4px;
    font-size: 1rem;
    letter-spacing: -.01em;
  }

  .session-card span,
  .empty-card {
    color: var(--muted);
    line-height: 1.45;
  }

  @media (min-width: 820px) {
    .session-card {
      padding: 16px 18px;
    }
  }
</style>
